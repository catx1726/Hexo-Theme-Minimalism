<%- partial('partials/head') %>

<body>

  <!-- <header id="menu-outer" class="header_container">
    <div id="menu-inner">
      <% Object.keys(theme.menu).forEach(key => { %>
      <a href="<%=theme.menu[key]%>"><%=key%></a>
      <% }); %>
    </div>
  </header> -->

  <main id="content-outer" class="main_container">
    <div id="content-inner" class="content_container">
      <div class="bar-line-dot">
        <div class="line"></div>
        <div class="circle"></div>
      </div>
      <%- body %>
      <div class="directory_container">
        目录
      </div>
    </div>
  </main>

  <footer id="bottom-outer" class="footer_container">
    <div id="bottom-inner">
      Site by <%=config.author%> using
      <a href="http://hexo.io">hexo blog framework</a>.
      <br>
      <a href="<%=config.root%>">Home</a>
    </div>
  </footer>

  <% if (theme.scripts !== undefined && theme.scripts.length > 0) { %>
  <!-- scripts list from theme config.yml -->
  <% theme.scripts.forEach(url => { %>
  <script src="<%=url%>"></script>
  <% }); %>
  <% } %>

</body>

</html>